# SKILL.md 维护指南

> **原则**：精简优先于膨胀。不要一味堆 prompt，历史验证无效的信息要定期清理。

## 维护策略（v2.9 实践）

1. **版本日志**：压缩为单行，不保留 v2.0-v2.8 的完整 changelog
2. **重复内容**：删除与 Aligner Prompt 重复的独立审核标准章节（故事审核 9 项等）
3. **完整代码**：大段模板代码压缩为"核心结构 + 模板引用"，不内联 180 行
4. **项目特例**：单次项目产生的陷阱保留通用规则，删除项目名+日期的特例描述
5. **尾部引用**：Drama Studio/Phase C/GitHub 等低关联内容压缩为引用摘要

## 清理流程

```python
# 分析文件结构
headers = re.findall(r'^## .+', content, re.MULTILINE)
# 按章节统计字符占比，识别臃肿区域
# 优先删除：重复段落 > 完整代码模板 > 项目特例 > 低关联引用
```

## 目标

- SKILL.md 保持在 50K 字符以内（当前 v2.9: ~48K）
- 每次维护至少减少 10% 冗余
- 核心流程（6 阶段工作流、Aligner Prompt、三文件架构）保持完整
